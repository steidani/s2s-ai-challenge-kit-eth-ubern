name: "base"
channels:
  - defaults
dependencies:
  - xarray
  # ML
  - tensorflow
  - pytorch
  # viz
  - matplotlib-base
  # - cartopy
  # scoring
  - xskillscore>=0.0.20  # includes sklearn
  # data access
  - intake
  - fsspec
  - zarr
  - s3fs
  - intake-xarray
  - cfgrib
  - nc-time-axis
  - pydap
  - h5netcdf
  - netcdf4
  - pip
  - pip:
    - climetlab >= 0.7.0
    - climetlab_s2s_ai_challenge >= 0.6.3
    - configargparse # for weatherbench
    - netcdf4==1.5.4
    - rdflib-jsonld==0.5.0
prefix: "/opt/conda"
